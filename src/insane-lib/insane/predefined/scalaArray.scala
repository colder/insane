package insane
package predefined

import annotations._

@AbstractsClass("scala.Array")
class scalaArray {
  @AbstractsMethod("scala.Array.<init>((_length: Int)Array[T])")
  def ____init__(_length: Int): scalaArray = {
    this
  }
}
